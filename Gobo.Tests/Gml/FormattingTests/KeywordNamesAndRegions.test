var _entry = _d.mod;
var _other = _b.data.mod;
var _struct = { static: 1, mod: 2, div: 3 };

#region Keyboard
if (_dt == DEVICE.keyboard)
	{
	keyboard_input();
	}
#endregion
#region Controller
else if (_dt == DEVICE.controller)
	{
	controller_input();
	}
#endregion

if (a) {
	one();
}
#region loose
two();
