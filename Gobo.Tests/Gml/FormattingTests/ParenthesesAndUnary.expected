foo++;
bar;
--foo;

++bar;

baz;
--qux;

x = !(!(!a));
y = (--(+a))++;
z = ((--(+a))++)();

// '!' should be outside
cond = !(condition || bar && baz);

// '!' should be inside
if (!(condition || bar && +baz)) {}
